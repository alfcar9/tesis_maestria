README
This files are for running the library of TSP results.

modified_dist.txt is for adding the geodesical distance to run the correspondent instances. We needed to modify the dist function of the R base package.
TSP_package.R allows to run the instances of TSLIB. 